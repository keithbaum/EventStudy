def addEvent(datasets, errorTypeIIParameters, eventWindow):
    for iteration in len( datasets ):
        for assetNumber in range( iteration.shape[0] ):
            for
    datasets[:,:,eventWindow.nonzero()[0]]+=
